[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = --strict-markers --tb=short
markers =
    integration: tests that call real external APIs (Gemini, etc.) — require .env
